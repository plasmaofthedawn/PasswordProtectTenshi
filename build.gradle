import org.apache.tools.ant.filters.ReplaceTokens // We use this to replace version in plugin.yml
 
apply plugin: 'java'

group = 'me.zonal'
version = '1.0'
archivesBaseName = 'passwordtenshi' // Put your artifactId here

def spigotVersion = '1.16.1-R0.1-SNAPSHOT'

repositories {
    mavenCentral()
    maven {
        url "https://hub.spigotmc.org/nexus/content/repositories/snapshots"
    }
}

processResources {
    filter ReplaceTokens, tokens: [
            "VERSION": version
    ]
}

dependencies {
    compileOnly "org.spigotmc:spigot-api:1.16.1-R0.1-SNAPSHOT"
}

compileJava.options.encoding = 'UTF-8'

sourceSets {
    main {
        resources {
            srcDirs "plugin", "src/main/java"
        }
    }
}

task sourcesJar(type: Jar) {
    classifier 'sources'
    from sourceSets.main.allSource
}

task javadocJar(type: Jar) {
    classifier 'javadoc'
    from javadoc.destinationDir
}

artifacts {
    archives sourcesJar, javadocJar // Build Sources and Javadoc Jar
}